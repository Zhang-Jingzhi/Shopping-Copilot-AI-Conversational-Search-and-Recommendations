# 2026-09-01 GitHub 同步与本地集成更新

本次只更新本地，没有推送 GitHub。当前工作分支为 `codex/system-integration`。
更新前版本 `653ab61` 保存在 `codex/backup-system-integration-20260901`。

## 分支与合入方式

| GitHub 分支 | 同步后提交 | 本次变化 |
|---|---|---|
| main | 4fae36a | 1 号 IntentResult 移除 route / route_reason，保留意图和查询解析 |
| master | da40b72 | 无新增提交 |
| feature/retrieval-reranking | 330ca6a | 无新增提交 |
| data/synthetic-proxy-3021 | c1d8e73 | 无新增提交 |
| ranking_pipeline | ab77248 | 合入本地尚未包含的 4afa5e1、f7ed695、ab77248：排序模型文件、商品词表、多轮状态更新和缓存 |

五个同名本地分支均与本次 fetch 后的 origin 对齐；缺少的同名本地分支已建立 tracking。
集成分支正常 merge `origin/ranking_pipeline`，保留原集成文档。
由于 main 与 ranking_pipeline 的历史不相连，使用 `cherry-pick -x 4fae36a`
引入 main 的新增改动，避免无关历史中旧模块覆盖集成版。其余来源的模块已存在且保持一致。
没有 Git 冲突，也没有为了合并而重写团队模块。

## 本地兼容处理

- 新版 3 号默认寻找 `official_kit/data/catalog.jsonl`。本地新增指向
  `../../retrieval-and-reranking/data/catalog.jsonl` 的符号链接，复用已经校验的 50,000 件商品数据。
  该链接已被根目录 `.gitignore` 忽略，不上传数据。新机器需安装商品数据并建立同一链接，或显式配置数据路径。
- `docs/integration/probe_contracts.py` 兼容 1 号移除 route 字段，输出
  `router_owns_route_selection=false`，不再访问不存在的属性。
- 新版上游跟踪了 `.cache/catalog_lexicon.json`。测试会按本机商品文件时间重新生成缓存；
  验证结束后恢复了上游缓存原文件，避免提交本机生成的元数据。以后运行时缓存仍可能显示为工作区改动。

## 验证

使用已安装的 Python 3.12.13。现有测试通过 70/70：Router 11、State 19、Retrieval 6、Ranking 34。
State 初次有一个品牌识别测试失败，原因是默认商品路径不存在；建立链接后通过。
Ranking 的 unittest discovery 必须从仓库根目录加 `-t .`，否则无法正确加载同级包。

在各模块目录运行 `python -B -m unittest discover -s tests -v`；
Ranking 在仓库根目录运行 `python -B -m unittest discover -s ranking_pipeline/tests -t . -v`。
这里的 python 必须是兼容版本，不能使用本机系统自带的 Python 3.9。

另外，用真实商品数据运行 4 条官方会话，配置为 lite + hybrid，开启 Router 和 State，关闭 policy。
19 次 respond 调用均无异常，响应格式、商品 ID 和新版 IntentResult 接入检查通过。

| 类型 | 样例 | 结果 |
|---|---|---|
| buying | public_0001 | 第 3 轮命中，第 1 名 |
| browsing | public_0006 | 10 轮内未命中 |
| intent_override | public_0002 | 第 3 轮命中，第 9 名 |
| boundary | public_0035 | 第 3 轮命中，第 1 名 |

这是兼容性运行检查，不能解释为完整效果验收、性能提升或新版 public-200 成绩。
没有执行 BGE exact、Qwen 实际推理、重新训练或全量 200 条评测。

机器可读来源与模块树校验见 `sync-manifest-2026-09-01.json`；
本地详细测试及运行结果在 `.local/sync-20260901/`。

## 仍需后续集成

本次同步不改变主流程：Router/State 仍不是检索的统一控制源。
诊断仍能观察到意图分歧、词表误识别、预算/排除条件适配丢失和 Agent 自身没有拒绝第 11 轮等问题。
这些属于后续接口与行为修正，不在本次 Git 更新中擅自重写。
